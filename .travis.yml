language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ms-codeigniter' ]; then cd .. && eval "mv $currentfolder generator-ms-codeigniter" && cd generator-ms-codeigniter; fi

